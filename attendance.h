#ifndef ATTENDANCE_H
#define ATTENDANCE_H

#include <QWidget>

class attendance : public QWidget
{
    Q_OBJECT
public:
    explicit attendance(QWidget *parent = 0);
    
signals:
    
public slots:
    
};

#endif // ATTENDANCE_H
